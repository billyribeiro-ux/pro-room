# proroom-NUCLEAR.json

- **path**: `evidence-folder/proroom-NUCLEAR.json`
- **kind**: json-capture
- **size**: 23,384,044 bytes (~22.3 MB)
- **role**: member (viewer) — mixed content. Determined from `meta.url` = `https://chat.protradingroom.com/?id=652754202ad80b3e7c5131e2&sl=1` (member-style room URL, no `/admin`), a mobile Pixel-9 UA, and the DOM containing viewer-side surfaces (admin trade alerts, roster, presentation split) but **no** author/compose-admin surfaces: `grep` for `appusersettings` / `avsettings` / `webcamholder` all return `false`. The room shows admin messages (`msg-box-adm` ×16) as *received content*, i.e. this is the member-facing render.
- **format/quality**: computed styles + rects (states/groups-equivalent). This is the richest single capture in the corpus: it carries BOTH `domHtml` (1,702,741 chars raw DOM+inline styles) AND `elements` (9,000 nodes, each with `{path, tag, class, attrs, rect{x,y,w,h}, style{computed}, before, after}`), plus `cssVariables` (294 root vars), `fonts` (16 loaded, 4 `faceRules`), `stylesheets` (41 entries, each `{href, rules[, blocked]}`), and a `modals` object.
- **surfaces documented**: main tabset (`#notesTabsContent`, `#files`, `#streamsTabsContent`, `#screensTabsContent` seen in embedded CSS), chat message stream (`app-st-message` ×200, `msg-box` ×118, `msg-box-adm` ×16), trade alerts / Q&A (`app-alerts` ×4, `alert-qa` ×53, `tradeColor` ×10), message actions (`msgMenu` ×104, `users-dropdown-options` ×101, `replyModal` ×51, "Add Reaction" ×100), reactions/emoji (regex `emoji|reaction` = true), user badges (`user-badge-img` ×218), roster (`room-roster` ×5, `presUser` ×3, `regUser` ×3, `rosterImg` ×4, `volumeControl` ×8), search (`st-searchbar` ×2), notes/files (`noteTabset` ×1, `noteDownload` ×6, `files-tabs` ×1, `files-badge` ×4), presentation stage (`presentation-box` ×1, a split-pane area), timestamps (`created-at` ×103), 7 `<textarea>` inputs.
- **maps to (our components)**: chat pane / message list + `msg-box-adm` styling (admin vs member message variants); trade-alert card + `tradeColor` BUY/SELL coloring; reply modal + reaction picker; message context menu (`msgMenu`); user badge rendering (image-based, see key finding); roster sidebar with presenter vs regular-user rows and per-user volume control; notes/files tab panes with download + badge counts; presentation/stream stage split layout; search bar; theme system (294 CSS vars incl. light/dark room + adm variants).
- **key findings** (cited):
  1. **Badges are IMAGES, not text.** `grep` of `domHtml` for `user-badge-img` returns 218 hits, and sampled nodes are `<... class="user-badge-img" src="https://i.imgur.com/9g1lJbw.png">` (also `EwVGWGS.png`, `6a4VEXZ.png`, `Tr4pmvT.png`). This CONFIRMS the corpus-wide warning that the real badges are `<img class="user-badge-img">` off imgur — NOT "New"/"Trial" text badges. `badge-success` appears only ×1.
  2. **Trade alerts carry a colored BUY/SELL token with a stable id.** A `tradeColor` node reads `class="tradeColor" id="id_6a297f25b90d4e6b500d42e6">BUY +…` — trade direction text lives inside a `.tradeColor` span keyed by an alert id. 10 `tradeColor` occurrences total.
  3. **Presentation stage is a resizable split pane.** The single `presentation-box` node is `class="presentation-box as-split-area" style="order: 2; flex: 0 0 calc(42.9833% - 4px)…"` — the stage occupies ~42.98% of a split layout (angular-split `as-split-area`), a concrete layout ratio for side-by-side comparison.
  4. **Palette / theme vars present.** `cssVariables.root` has 294 entries including `--success:#00bc8c`, `--darkTheme-msgs-bg-adm:#0f2e43`, `--lightTheme-roster-bg-adm:#e1e1e1`, `--darkTheme-msg-border-color:#f4f4f4`, `--file-list-even-bg:#f4f4f4`, `--modal-btn-success-border:#92d528` — i.e. distinct light/dark AND admin-vs-member (`-adm`) surface colors are captured as authority.
  5. **Angular render fingerprint.** Nodes carry `_ngcontent-ng-cNNNNNNNN` attributes (multiple distinct component ids e.g. `ng-c2028866615`, `ng-c1922465750`, `ng-c3761163150`), confirming the source app is Angular with per-component scoped styles — relevant when mapping scoped CSS boundaries to our Svelte components.
- **notes**:
  - **HONEST GAP — the `modals` object is unreliable.** It lists 8 named keys (`Connectivity Check`, `General Settings`, `Archives`, `Alert Logs`, `Chat Logs`, `Transcript History`, `Manage Muted Users`, `Manage Followed Users`), but ALL 8 captured `html` values are byte-identical (`m.every(x=>x.html===m[0].html)` = true), each 1,365 chars / 16 elements, and the shared content is a generic user-profile modal (`class="modal-content"` → `edit-user-avatar`, gravatar `avatar/undefined`, `badge badge-danger">Offline`, `btn-close-white`). The capture grabbed whatever single modal was open and stamped it under all 8 names. **Do NOT treat these named modals as authority for their titles** — only the one generic offline-user modal is actually present.
  - **Best-authority flag:** for the overall room shell (chat + alerts + roster + presentation + tabs) this file is the strongest single JSON authority in the corpus because it pairs raw `domHtml` with 9,000 computed-style/rect `elements` and full CSS-var + font + stylesheet context in one capture.
  - `presUser`/`regUser` appear in `domHtml` (×3 each) but a direct `elements[].class` exact-match filter for `presUser` returned none — those tokens live in roster CSS selectors / nested markup rather than as a standalone element class; verify roster row structure against `domHtml`, not the `elements` class index.
  - This is a `.json` raw capture, so it IS authority (rendered DOM + computed styles), not prose — supersedes any `.md` claim it conflicts with.
