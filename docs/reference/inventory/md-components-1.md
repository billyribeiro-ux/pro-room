# md-components-1 — inventory (Phase 1 forensic)

> **Corpus-state caveat (verified):** All three assigned files are **absent from the working tree** — `git status` shows them as `D` (deleted) alongside the rest of `docs/reference/*.md`. They still exist in `HEAD` (commit `435283b`, originally authored in `19ea93b` "Build pro-room trading room…"). I read each from `git show HEAD:<path>`. Byte sizes are `git cat-file -s HEAD:<path>`. Confirmed via `git ls-files | grep component` (tracked) + `find` on disk (not present).
>
> **Authority reminder:** every file below is **kind: md-analysis — SECONDARY/PROSE, NOT authority**. Each is a hand-written summary of Angular 17 rendered `files/fileNN.html` DOM dumps. The cited `fileNN.html` raw DOM dumps are the actual authority and MUST be verified against; rendered capture wins over this prose. These files self-describe their extraction method ("Angular cruft stripped: `_nghost*`, `_ngcontent*`, `ng-star-inserted`, `ng-reflect-*`, empty `<!---->`") — meaning they are already a lossy, edited derivative, not a raw capture.

---

# components-alert-modals2.md

- **path**: docs/reference/components-alert-modals2.md (in HEAD only; deleted in working tree)
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps (`files/file28.html`, `file25.html`, `file26.html`, `file29.html`)
- **size**: 14273 bytes
- **role**: admin/presenter (alert-authoring surfaces — only presenters/admins post/search/schedule alerts). Determined from: content is entirely alert-composer/manager modals, no member-only markers.
- **format/quality**: prose analysis of raw DOM+inline structure (simplified indented trees + control-inventory tables + explicit divergence lists). NOT computed-styles/rects, NOT a screenshot.
- **surfaces documented**: 4 Bootstrap modals — (1) `app-alerts-advanced-search` → `AdvancedSearchModal.svelte`; (2) `app-scheduled-alerts-modal` → `ScheduledAlertsModal.svelte`; (3) `app-alert-send-report-modal` → NEW `AlertSendReportModal.svelte`; (4) `app-alert-filter-modal` → `AlertFilterModal.svelte`.
- **maps to (our components)**: `AdvancedSearchModal.svelte`, `ScheduledAlertsModal.svelte`, `AlertFilterModal.svelte` (all named as existing pro-room components with divergence lists), plus a NEW `AlertSendReportModal.svelte` (flagged "not yet built"). Reason: the doc explicitly names each `.svelte` target and gives a "Divergences from current pro-room implementations" section per modal.
- **key findings** (cited):
  1. All four share the Bootstrap shell `.modal.fade > .modal-dialog > .modal-content > {.modal-header, .modal-body, .modal-footer}`; header `<h5>` title + `button.btn-close.btn-close-white[data-bs-dismiss="modal"]` (stated in the intro paragraph).
  2. AdvancedSearch trader/room pickers are **Bootstrap dropdowns** (`button#selectTraderDropdown.dropdown-toggle[data-bs-auto-close="outside"]` + `ul.dropdown-menu.w-100 > li > a.dropdown-item`), NOT native `<select>` — and the doc flags pro-room wrongly uses `<select multiple size=3>`. Placeholders `--Select Traders--` / `--Select Rooms--`. 20 trader items + 3 room items enumerated in DOM order.
  3. ScheduledAlerts dialog is `.modal-dialog.modal-xl` with `.text-white`; table is `table.table-striped.text-white.w-100` with exactly 5 cols: **Date / Time · Sender · Alert · Repeat · Actions**; snapshot `<tbody>` is a single empty `<!---->` — **no rendered empty-state row** (so row-action markup is INFERRED, an explicit gap).
  4. AlertSendReport captured in **loading state only**: `h5 > i.fas.fa-spinner.fa-spin "Loading..."` then four `<!---->` placeholders where resolved report sections mount. The doc admits the 4 resolved sections (delivery summary / pie chart / recipient breakdown) are INFERRED from the modal name — "only the count (4) and their mount order are known."
  5. AlertFilter: `.modal-body.pt-1` with `input#show-alerts[type=checkbox]` label "Only show alerts from these people:", empty state is literal `<p>List is empty.</p>`; footer `justify-content-between` with a trailing `<!---->` = conditional 2nd button (INFERRED Save/Add).
- **notes**: Filename suffix "2" implies a sibling `components-alert-poll.md` (below) and likely a `components-misc-modals.md` (seen in `git ls-files`, NOT in this task). Heavy reliance on `<!---->` placeholder interpretation = many INFERRED claims explicitly labeled by the author; those must be verified against a populated capture, not trusted. Best authority for these 4 modals' *structure* only if the cited `fileNN.html` dumps confirm; badge/text specifics unverified here.

---

# components-alert-poll.md

- **path**: docs/reference/components-alert-poll.md (in HEAD only; deleted in working tree)
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps (`files/file12.html`, `file13.html`, `file15.html`)
- **size**: 12126 bytes
- **role**: admin/presenter (alert composer, poll creator, alert logs — authoring/moderator surfaces). Determined from: content is post-alert / create-poll / logs, none are member-consumption views.
- **format/quality**: prose analysis of raw DOM structure (simplified box-drawing trees + field/control inventories + a per-component mapping/divergence section). NOT computed styles/rects, NOT a screenshot.
- **surfaces documented**: 3 components — (1) `app-post-alert-modal` (file12) the alert composer; (2) `app-poll-modal` (file13) the poll creator (a draggable windowed panel, NOT a Bootstrap modal); (3) `app-alert-logs-modal` (file15).
- **maps to (our components)**: `web/src/lib/components/modals/PostAlertModal.svelte`; `PollModal.svelte` (+ `PollPanel.svelte` for voting/results, noted as our own with no source equivalent); `modals/AlertLogsModal.svelte`. Reason: explicit "Mapping to pro-room + divergences" section names each.
- **key findings** (cited):
  1. PostAlert type is chosen by **3 Bootstrap tabs**, not a select: `a#nav-tab-text "Text Alert"` (active) / `a#nav-tab-url "Text Url"` / `a#nav-tab-img "Image / GIF / Video"`. Text tab = single `textarea[rows=10, placeholder="Alert Text..."]`; URL tab = `input[type=url]` (icon-prepended `#addon-url` `fa-link`) + `textarea[rows=2]`; Image tab = url input OR real hidden file input `#fuploadAlert[type=file multiple accept="image/*" hidden]` + drop zone `#filedragAlert` + thumb list `#fileListAlert` + `textarea[rows=2]`.
  2. PostAlert footer has **FIVE** persistent checkboxes with exact ids: `keepOpenChk`, `postOnXChk`, `alert-push-label` ("Don't send to push notification?"), `alert-non-trade-label` ("Non-trade alert? (Different Sound)"), `alert-legal-disclosure-label` ("Add Legal Disclosure?"). Send = single `button.btn.btn-success "Post Alert"`. Doc flags pro-room only has TWO checkboxes (missing Post on X, suppress push, Legal Disclosure).
  3. Author's explicit note: **no symbol/side/ticker/scheduling field** exists in the composer — it is a free-form note + link/image; type = the active tab. (Important structural fact for side-by-side.)
  4. PollModal is a **draggable windowed panel** `app-poll-modal#pollModalCompHolder.pollModalHolder` with its own titlebar (`#pollPanelTitlebar` + Minimize/Maximize/Close `.poll-panel-btn` buttons), NOT a Bootstrap modal. Two tabs: `#sendpoll` "Create New Poll" (active) + `#savedPolls` "Pre-Canned Polls". Numbered 1/2/3 steps via `span.label.label-warning`. Choice model = single `input#pollChoiceTxt` + "Add Choice" (`fa-plus-circle`) appending to an `<ol>` (NOT per-row inputs). `input#anonymous-poll[type=checkbox]`. Footer: "Save To Canned" (`fa-floppy-o`) + "Send Poll". Doc flags pro-room lacks the entire Pre-Canned tab (biggest gap) and uses shared `Modal.svelte` (not draggable).
  5. AlertLogs (`#alerts-logs-modal`): `button.btn-primary.my-2 "Reload Log List"` above a `.list-group`; each `.list-group-item.list-group-item-action` = two rows — date `<strong.fw-bold>` (sample "Oct 22, 2023") + `<strong>By: </strong><i>admin@protradingroom.com</i>`; footer secondary "Close". Doc notes pro-room hard-codes `logs=[]` with a disabled Reload.
- **notes**: PollModal's draggable-panel identity (jQuery-UI style titlebar, min/max/close) is a significant divergence from our shared-Modal approach — flag for reviewers. `PollPanel.svelte` (voting/results renderer) has **no source counterpart** in these snapshots per the author. Sample email `admin@protradingroom.com` and date are illustrative single samples — verify against raw `file15.html`.

---

# components-media-misc.md

- **path**: docs/reference/components-media-misc.md (in HEAD only; deleted in working tree)
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps (`files/file22.html`, `file23.html`, `file30.html`, `file31.html`, `file33.html`, `file34.html`)
- **size**: 12627 bytes
- **role**: presenter/mixed (screen-share/recording previews + connectivity/RTE modals + global toast & audio-sink fragments). Determined from: screenshare-preview + rec-preview are presenter media surfaces; toast/audio fragments are app-global (mixed).
- **format/quality**: prose analysis with **verbatim HTML snippet blocks** (not just trees) + control tables + a final "NEW vs existing" summary table. NOT computed styles/rects, NOT a screenshot. Underlying libs noted: Bootstrap 5 + `ngb*` dropdowns + Font Awesome + jQuery-UI draggable/resizable.
- **surfaces documented**: 6 items — (1) `app-screenshare-preview` (file22); (2) `app-rec-preview` (file23, NEW); (3) `app-webrtc-troubleshooter` (file30) → `ConnectivityCheckModal.svelte`; (4) `app-rich-text-editor` (file31, NEW); (5) `file33.html` toast container (fragment, NOT a component); (6) `file34.html` hidden `<audio id="webcam">` remote-audio sink (fragment, NOT a component).
- **maps to (our components)**: `ScreenStage` + `WebcamHolder` (drag/resize chrome) for screenshare-preview; NEW `RecPreview.svelte`; `ConnectivityCheckModal.svelte` (existing); NEW `RichTextEditorModal.svelte` (+ pairs with `NotesPanel`); toast host (`toast` store); remote-audio sink in call plumbing. Reason: explicit per-section "Map to pro-room" + a summary table with a New? column.
- **key findings** (cited):
  1. `app-screenshare-preview`: `#screenshareLocalPreviewHolder.card.webcamsHolderScreen.ui-draggable.ui-draggable-handle.ui-resizable`; source picker is `ngbDropdown` (`button#dropdownBasic1.dropdown-toggle.btn.btn-outline-dark`, empty in snapshot); close = `span.float-right.p-2 > i.fas.fa-times`; video `<video autoplay id="webcamScreenLocalPreview" class="webcamPreviewScreen">` (no `controls`/`muted`); **8 jQuery-UI resize handles** (n/e/s/w/ne/se/sw/nw) but only `se` active (`display:block` + grip icon). Doc advises pro-room NOT pull jQuery-UI — use pointer action + single `se` corner handle.
  2. `app-rec-preview` (NEW): `#recLocalPreviewHolder.card.recsHolderScreen` (note `recsHolderScreen`, NOT `webcamsHolderScreen`); static title "Recording Preview. (DELAYED UPTO 20s)"; two icons `fa-times` (close) + `fa-expand` (fullscreen); body shows paused state `<h4>Recording paused.</h4>`; **no** `ui-draggable`/`ui-resizable` classes → positioned, not drag/resize.
  3. `app-webrtc-troubleshooter` (file30) → `ConnectivityCheckModal.svelte`: `.modal-dialog` pinned `style="max-width:540px"`; title `<h3>` "Connectivity/Mic Troubleshooter"; exactly 4 `.status-item` rows — **UDP Enabled · TCP Enabled · STUN Server Connectivity · TURN Server Connectivity** — each with `span.status-icon.pending "●"`; footer 3 buttons: `.btn-primary fa-play "Start Test"`, `.btn-success fa-copy "Copy Results"`, `.btn-secondary "Close"`. Author flags: title says "Mic" but **no mic level-meter UI** rendered (body is network-only) — honest gap.
  4. `app-rich-text-editor` (file31, NEW): `#rteModal` Bootstrap modal, title "Rich Text Editor"; body is a single empty `<div id="msgTxtContainer">` — the **toolbar + contenteditable are injected by the editor lib at runtime and are ABSENT from the snapshot** (explicit gap — Summernote-style); footer `.btn-secondary "Close"` + `.btn-primary <span>Send</span>`. "Send" framing suggests it doubles as a chat composer.
  5. Two fragments are explicitly NOT components: file33 = `div.overlay-container[aria-live=polite] > #toast-container.toast-top-right.toast-container` (ngx-toastr host → maps to pro-room `toast` store target); file34 = `<audio autoplay hidden id="webcam">` (WebRTC remote-audio sink → call plumbing, not UI).
- **notes**: This is the most self-honest of the three — it repeatedly flags what is INJECTED-AT-RUNTIME / ABSENT-FROM-SNAPSHOT (RTE toolbar, dropdown items, active-recording video) rather than inventing it. Class-name specifics (`webcamsHolderScreen` vs `recsHolderScreen`, `webcamPreviewScreen`, `max-width:540px`) are concrete and checkable against the raw `fileNN.html` dumps — verify before use. Two NEW components proposed (`RecPreview.svelte`, `RichTextEditorModal.svelte`).

---

## Cross-file notes
- All three are the same author/era (identical "Angular cruft stripped" preamble, identical divergence-table style) — a consistent SECONDARY analysis set covering Angular components `file12`, `13`, `15`, `22`, `23`, `25`, `26`, `28`, `29`, `30`, `31`, `33`, `34`.
- Recurring reliability caveat for reviewers: wherever these docs describe *populated* content (report sections, scheduled-alert rows, RTE toolbar, dropdown items, active recording video), it is **INFERRED from empty `<!---->` placeholders or lib mount points**, not observed. Treat those as gaps to confirm against a real capture; trust only the empty-state / static-chrome structure, and even that only after checking the cited raw `files/fileNN.html`.
- Sibling files present in `git ls-files` but outside this task: `components-misc-modals.md`, `components-settings.md`, `components-user-pm.md` — likely the rest of this component-analysis series.
