# frag-appusersettings — inventory

Slug: `frag-appusersettings`. Two HTML DOM dumps from `docs/reference/visual-evidence-deep/fragment-pages/`. Both are wrapped in an "evidence-wrap" chrome page (sticky banner with `evidence-pill`s, outer bg `#092335`) but the inner payload is a **raw Angular DOM dump with inline styles**, not computed-style/rect JSON. Authority = the raw DOM below, not any prose.

---

# appusersettingsmodal.html.html
- **path**: docs/reference/visual-evidence-deep/fragment-pages/appusersettingsmodal.html.html
- **kind**: html-dom-dump (raw DOM + inline/`<style>` CSS; Angular render). Wrapped in an evidence-viewer shell.
- **size**: 511,894 bytes (~500 KB) — `wc -c`
- **role**: member — this is the self-service **App User Settings** modal (own theme/layout/chat/alert prefs). No admin/moderator-only controls present; no `msg-box-adm`. (determined: only tabs are General/App/Alert/Chat Settings, all personal prefs.)
- **format/quality**: raw DOM + inline styles. Angular component (`_ngcontent-ng-c124836360` host attrs). NOT a computed-styles/rects capture.
- **surfaces documented**: the `app-user-settings-modal` — a Bootstrap `.modal.fade` with `.modal-dialog/.modal-content/.modal-header/.modal-body/.modal-footer.text-center`, a 4-tab pill set (`data-bs-toggle="tab"`), and the full settings form.
- **maps to (our components)**: our User Settings modal / preferences panel (theme picker, room layout, chat colors, alert prefs). Reason: 1:1 field set below.
- **key findings** (cited):
  - **4 tabs**, ids `user-app-settings-tab` / `user-alert-settings-tab` / `user-chat-settings-tab` inside `#userSettingsTab` / `#userSettingsTabContent`, labels: **General Settings, App Settings, Alert Settings, Chat Settings** (grep of `id="..."` in `.modal.fade` window + label text extraction).
  - **General tab** = Theme + Layout + Colors: `colorTheme` radios `app-light-theme`/`app-dark-theme` ("Light Theme"/"Dark Theme"); `roomLayout` radios `chat-alerts-left/top/right/bottom` ("Chat and Alerts left/top/right/bottom"); checkbox `pm-window-layout` ("PM logs on the right"); **4 `type="color"` pickers** `chat-text-color`, `chat-username-color`, `chat-bg-color`, `chat-ticker-color` (labels Text/Username/Background/Ticker Color) + a `type="number"` `chat-text-size` ("Text Size"); **Reset** and **Save changes** buttons. (extracted from `.modal.fade` DOM window.)
  - **App tab** checkboxes: `app-donot-disturb` (Do not disturb / "Don't Disturb"), `app-recording-start-sound` (Start recording sound), `app-recording-stop-sound` (Stop recording sound), `app-reactions-popup` (Reactions Response), `app-reactions-popup-qa` (Reactions QA Response), `app-disable-video` (Disable/Enable Video → "Enabled"), `app-speech-reco-overlay` (Show Closed Captions Overlay → "Enabled"); plus an **"Edit my Info and Avatar"** button. (input-id extraction.)
  - **Alert tab**: `alertTextMode` radios `alert-regular-mode`/`alert-compact-mode` ("Regular Mode"/"Compact Mode"); checkboxes `alert-popup-donot-disturb` (Alert / QA Popup), `alert-donot-disturb` (Alert sound), `qa-donot-disturb`. (input-id extraction.)
  - **Theme/CSS provenance**: `<style>` opens with Bootswatch **v4.3.1** + Bootstrap **v4.3.1**; `:root` palette `--primary:#375a7f`, `--success/--green:#00bc8c`, `--danger:#E74C3C`, `--warning:#F39C12`, `--dark:#303030`; FontAwesome linked via `@fortawesome/fontawesome-free/css/all.min.css`. This is the **Darkly** Bootswatch theme (word "Theme" appears 92×). (head + `:root` block read directly.)
- **notes**: The literal string `appusersettingsmodal` appears ONLY in the evidence-shell `<title>`/`<strong>`/`evidence-pill` (3 hits) — there is NO DOM `id="appusersettingsmodal"`; the real element is `.modal.fade` (Angular `app-user-settings-modal`). Evidence pills claim "1 app tags / 20 modal ids / 25 audited gaps". This is the **best single-surface authority for the User Settings modal** — self-contained and small; prefer it over the same modal embedded in important-doc.

---

# important-doc.html.html
- **path**: docs/reference/visual-evidence-deep/fragment-pages/important-doc.html.html
- **kind**: html-dom-dump (raw live Angular DOM + inline styles), evidence-shell wrapped.
- **size**: 6,342,526 bytes (~6.05 MB) — `wc -c`
- **role**: mixed / full-room member capture. It is the **entire `app-root` → `app-room` shell** with every major component + modal instantiated. No `msg-box-adm`, `presUser`, `regUser`, `adminUser`, or `moderator` strings (all count 0), consistent with a member session, but presenter/admin *components* (`app-presenter-cams`, `app-post-alert-modal`, `app-poll-modal`) are present in the DOM tree. (determined by grep counts.)
- **format/quality**: raw DOM + inline styles, **Angular `ng-version="17.3.12"`** (95× `_nghost`, 21,529 `ng-c…` scoped-style hashes). Live capture, NOT computed-styles/rects. Chat messages are present as **50 `<app-st-message>` component instances** but rendered leaf text (usernames, `created-at`) did not extract as plain text (content is in nested ng-scoped spans / possibly sparsely populated).
- **surfaces documented**: whole room — nav/roster, chat, alerts, notes, webcams, presentation, and the modal fleet.
- **maps to (our components)**: essentially our entire room shell. Component census (via `<app-*` tag count):
  - `app-root`, `app-room`, `app-room-roster`, `app-chat`, `app-alerts`, `app-webcam-holder`, `app-presentationarea`, `app-presenter-cams` (×2), `app-roomscroller` (×2), `app-note` (×6), `app-st-message` (×50).
  - Modals: `app-user-settings-modal`, `app-av-settings-modal`, `app-user-info-modal`, `app-followed-users-modal` (×2), `app-play-youtube-modal`, `app-post-alert-modal`, `app-poll-modal`, `app-chat-logs-modal`, `app-debug-log-modal`.
- **key findings** (cited):
  - **63 modal ids** claimed; DOM `id=…modal…` set includes: `user-settings-modal`, `av-settings-modal`, `user-modal`, `followedUsersModal`, `mutedUsersModal`, `replyModal`, `rteModal`, `alert-modal`, `alertQAModal`, `alert-filter-modal`, `alert-send-report-modal`, `alerts-advanced-search-modal`, `alerts-logs-modal`, `scheduledAlertsModal`, `all-user-pm-modal`, `chat-logs-modal`, `debug-log-modal`, `play-youtube-modal`, `pollModalCompHolder`, `session-control-modal`, `webrtc-troubleshooter-modal`, `mobileAppInfoModal`. (regex over `id="…modal…"`.)
  - **Tabs**: `nav-link` text set = **Main Chat, Off Topic** (chat channels) + **User Settings, App Settings, Alert Settings, Chat Settings** (settings) + **Create New Poll**. `mainTabs`=15 / `mainTabset`=13 lines, `noteTabset`=12, `files-tabs`=7. (nav-link text extraction.)
  - **Chat volume markers**: `app-st-message`=50, `msgMenu`=52, `users-dropdown-options`=54, `alert-qa`=52, `created-at`=53, `tradeColor`=15 (`class="tradeColor"` present), `msg-box`=50, `app-st-message`=100 line-hits. `flex-row-reverse`=3 (self/right-aligned messages), `replyModal`=1. (grep counts.)
  - **Media/presentation**: `app-webcam-holder`, `app-presentationarea`, `app-presenter-cams`; `webcam`=16, `presenter`=10, `presentation-box`=2; roster is `app-room-roster` → `room-roster-list` inside a `navbar…mainAppNav` sidebar. A `.video-screen-container` / `.is-fullscreenshare` / `.overlay-userID-container` CSS block exists (screen-share/fullscreen styling). (grep + CSS block read.)
  - **`user-badge-img`** appears **only in CSS rules** here (badge sizing `width:auto;height:100%;max-height:20px`, `.user-badge/.new-badge/.trial-badge` hidden in `-sm` columns); **0 populated `<img class="user-badge-img">` in the DOM body** of this dump. So this file confirms badge *styling/existence* but does not itself render badge image srcs. (regex for `<img…user-badge-img…>` = 0.) Corroborates the CLAUDE.md note that real badges are `<img class="user-badge-img">` (rule) rather than text.
- **notes**: This is the **master superset dump** — every other fragment (including appusersettingsmodal) is a subset of what lives here. Use it as the structural/relationship authority (component nesting, modal roster, tab set). For a focused per-modal read, prefer the smaller dedicated fragment. Same Bootswatch Darkly v4.3.1 CSS + FontAwesome `all.min.css` as the other file. Evidence pills: "36 app tags / 63 modal ids / 32 audited gaps".
