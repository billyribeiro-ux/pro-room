# md-components-2 — inventory (slug: md-components-2)

> **Corpus note / honest gap:** All three assigned files are **deleted from the working tree** (`git status` shows `D docs/reference/components-misc-modals.md`, `... components-settings.md`, `... components-user-pm.md`) but are still tracked at `HEAD`. They were read in full via `git show HEAD:<path>`. Sizes below are the git blob sizes (`git cat-file -s`), which differ from the stale prompt sizes (the prompt listed them at a non-existent flat path).
>
> **All three are `kind: md-analysis` — SECONDARY/PROSE, NOT authority.** They must be verified against the raw JSON captures and HTML DOM dumps. Per the standing directive: rendered capture ALWAYS wins over these prose files, which are known to contain errors (e.g. the "New"/"Trial" TEXT-badge fabrication elsewhere in the corpus; real badges are `<img class="user-badge-img">`). None of these three files cite computed styles or rects — they are hand-transcribed Angular DOM trees with Angular cruft stripped, so their class/id lists are the useful part and their prose "divergence" judgments are opinion.

---

# components-misc-modals.md
- **path**: docs/reference/components-misc-modals.md (tracked at HEAD; deleted from working tree)
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps
- **size**: 7339 bytes (git blob)
- **role**: mixed — member-visible modals (user-info, mobile-app, chat-logs) plus admin/host tools (play-youtube, debug-log, session-control). Determined from the modal purposes stated in the prose (e.g. "Admin/host broadcast tool", "admin session-management tool") — NOT from any captured `meta.role`.
- **format/quality**: prose analysis — hand-transcribed markup trees from Angular 17 rendered snapshots with Angular cruft (`_nghost*`, `_ngcontent*`, `ng-star-inserted`, `ng-reflect-*`, empty `<!---->`) stripped. No computed styles, no rects. Self-declared source: `files/file7,8,11,16,17,14.html`.
- **surfaces documented**: 6 Bootstrap modals — `app-user-info-modal` (file7), `app-play-youtube-modal` (file8), `app-debug-log-modal` (file11), `app-session-control-modal` (file16), `app-mobile-app-info-modal` (file17), `app-chat-logs-modal` (file14, shell only).
- **maps to (our components)**: `UserInfoModal.svelte` (file7); **NEW** `PlayYouTubeModal.svelte` (file8); **NEW** `DebugLogModal.svelte` (file11); **NEW** `SessionControlModal.svelte` (file16); `MobileAppInfoModal.svelte` (file17); `ChatLogsModal.svelte` (file14). Mapping reasons are the doc's own component-name assignments.
- **key findings** (cited):
  1. All six share the Bootstrap modal shell `.modal.fade > .modal-dialog > .modal-content > {.modal-header, .modal-body, .modal-footer}` with a `btn-close btn-close-white` dismiss (stated in the file header block, lines under the title).
  2. `app-user-info-modal` footer actions cited as **@Mention / Private Chat / Follow / Mute / Close**, with semantic Bootstrap colors: Follow=`btn-outline-info`, Mute=`btn-outline-warning`, Close=`btn-primary`; avatar is a gravatar `<img src=.../avatar/undefined?d=mm&s=80>` and the `modal-title` carries only a `span.badge.badge-danger "Offline"` (name empty/data-driven in snapshot). — Section 1 markup tree.
  3. `app-mobile-app-info-modal` cites **real store hrefs**: Google Play `com.bellesoft.stprotradingroom` and App Store `id1278652736`, laid out as store-badge images (`/assets/images/google-play-badge.png`, `/assets/images/iosAppStore.svg`); source title "Download our mobile apps" vs our "Mobile App Info". — Section 5.
  4. `app-chat-logs-modal` row model = three stacked `div`s with `strong.fw-bold` labels: **Date** (e.g. "Jun 12, 2026"), **By: `<email>`** (e.g. admin@protradingroom.com), **Channel: `<main|offTopic>`**; a `.btn.btn-primary "Reload Log List"` is active in source (disabled in ours). — Section 6.
  5. `app-debug-log-modal` is `modal-lg` with a single readonly `textarea#debugLogModalTxt [rows=1000]` and its `.modal-footer` sits as a **sibling of `.modal-content`** (outside it) in source — a structural quirk explicitly flagged. — Section 3.
- **notes**: Marks 3 NEW modals to build (PlayYouTube, DebugLog, SessionControl). Class/id/href lists are useful leads but every claim (especially the `undefined` gravatar and the "empty body" snapshots) must be re-confirmed against file7/8/11/16/17/14 raw HTML DOM dumps before use. This is the only assigned file that covers the misc/admin-tool modal set.

---

# components-settings.md
- **path**: docs/reference/components-settings.md (tracked at HEAD; deleted from working tree)
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps
- **size**: 11601 bytes (git blob)
- **role**: mixed — `app-user-settings-modal` and `app-av-settings-modal` are member-facing settings; the av-settings modal has a hidden `#presenter-audio-video-settings` panel (presenter). file5 is the main navbar volume dropdown. Determined from the panel ids and prose ("presenter device inputs"), NOT from a captured role.
- **format/quality**: prose analysis — hand-transcribed Angular 17 snapshot markup, Angular cruft stripped, real `id`/`class`/`role`/`aria` kept. Includes several transcribed tables of control ids/values/labels. No computed styles/rects. Self-declared sources: `file9.html`, `file10.html`, `file5.html`.
- **surfaces documented**: `app-user-settings-modal` "General Settings" (file9, 3 tabs: App / Alert / Chat); `app-av-settings-modal` "Audio/Video Settings" (file10, User tab + hidden Presenter panel); file5 main navbar `nav.mainAppNav` **Volume dropdown** (`#dropdownVolume` → `.dropdown-menu.volumeControl`).
- **maps to (our components)**: **NEW** `SettingsModal.svelte` (file9 — no equivalent yet); existing `modals/AVSettingsModal.svelte` (file10); `Nav.svelte` / `RoomTopNav.svelte` volume popover (file5). Reasons are the doc's own mapping-summary table.
- **key findings** (cited):
  1. `app-user-settings-modal` tab set = `#userSettingsTab.nav.nav-tabs` with tabs **App Settings** (`#user-app-settings-tab`, active), **Alert Settings** (`#user-alert-settings-tab`), **Chat Settings** (`#user-chat-settings-tab`); footer single `btn-secondary "Close"`. — Section 1 markup tree.
  2. App-Settings appearance controls are transcribed with **binding-expression `value`s, not real values**: `chat-text-color`→`chatStyle.color`, `chat-username-color`→`chatStyle.usernameColor`, `chat-bg-color`→`chatStyle.bgColor`, `chat-ticker-color`→`chatStyle.tickerColor`, `chat-text-size`(number)→`chatStyle.fontSize`. An explicit NOTE warns only the **chat** appearance set is present in file9 — alert/presenter color sets are NOT in this snapshot (contradicts prompts that assume them). — Section 1, "Colors & Size".
  3. Radio groups cited with exact ids/values: Color Theme `name=app-color-theme` (`app-light-theme`=`Light Theme`, `app-dark-theme`=`Dark Theme`); Room Layout `name=roomLayoutOptions` (`chat-alerts-left/top/right/bottom`); plus `#pm-window-layout` checkbox "PM logs on the right". Numerous DND checkbox ids listed per tab (e.g. `app-donot-disturb`, `alert-popup-donot-disturb`, `chat-gif-donot-disturb`). — Section 1 tables.
  4. `app-av-settings-modal` = two panels: visible `#user-audio-video-settings` (Disable Video nav toggle + `select#speakers-device` with inline **Test** `fa-volume-up`) and hidden `#presenter-audio-video-settings` (`select#audio-deviceList`, `select#video-deviceList`, `button.btn-primary "Change Devices"`); footer **Save** (`btn-success`) + **Close**. Divergence noted: our AVSettingsModal flattens speakers/mic/cam into one body and lacks the presenter "Change Devices" apply step. — Section 2.
  5. file5 is explicitly **NOT a settings modal** — it is the rendered `nav.navbar.mainAppNav` Volume dropdown: `<input type=range>` `audiovolslider` (0–100) + Mute + `.room-sound-options` toggles (`alert-donot-disturb`, `qa-donot-disturb`, `non-trade-donot-disturb`, `chat-donot-disturb`, `presentation-subtitles`, `app-donot-disturb`) that **duplicate** the user-settings sound toggles. — Section 3.
- **notes**: Best single lead for the settings/AV surface control inventory. The appearance-token table records Angular *bindings*, so it cannot be used for default color values — those must come from computed styles in a JSON capture. The self-flagged "chat-only appearance set" note is an important correction to any prompt assuming alert/presenter styling exists in file9. Verify tab ids and the hidden presenter panel against file9/file10 raw DOM.

---

# components-user-pm.md
- **path**: docs/reference/components-user-pm.md (tracked at HEAD; deleted from working tree)
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps
- **size**: 11136 bytes (git blob)
- **role**: member — reply, alert-Q&A, all-user PM list, private-chat panel, muted/followed user modals are all member messaging surfaces. AlertQA answering is noted as admin-gated in *our* implementation, not in the ST capture. Determined from surface descriptions, NOT a captured role.
- **format/quality**: prose analysis — hand-transcribed Angular 17 markup trees (box-drawing tree notation), Angular cruft stripped. No computed styles/rects. Several surfaces captured only in loading/empty state (noted). Self-declared sources: `file18/19/27/32/20/21.html`.
- **surfaces documented**: `app-reply-modal` (file18), `app-alert-qa-modal` (file19), `app-all-user-pmmodal` (file27), `app-privchat` panel (file32), `app-muted-users-modal` (file20), `app-followed-users-modal` (file21).
- **maps to (our components)**: `modals/ReplyModal.svelte` (file18, ORPHANED); `AlertQaModal.svelte` (file19, WIRED in AlertFeed); **NEW** `AllUserPmModal.svelte` (file27); **NEW** `PrivateChat` panel (file32); `modals/MutedUsersModal.svelte` (file20); `modals/FollowedUsersModal.svelte` (file21). Reasons are the doc's own summary table.
- **key findings** (cited):
  1. Shared composer pattern across Reply and Alert-QA: a `.textSendDiv` row with `textarea.txt-area.form-control` (`#textAreaReplyTxt` / `#textAreaQATxt`, `rows=1`, placeholder "Type your message here.." / "Type your question here...") plus `.textAreaBtnsCol` with `span.textAreaBtns > i.far.fa-smile` (Add Emojis) and `i.fas.fa-image` (Upload an Image). **No explicit Send button in source** — submit is Enter-on-textarea. — Sections 1 & 2.
  2. `app-reply-modal` title uses `span.do-private-reply` rendering `<strong>:</strong><div></div>` (dynamic target name); footer is **Close only**. Ours is ORPHANED (not referenced anywhere) and adds an explicit Send button + static "Reply" title. — Section 1.
  3. `app-alert-qa-modal` embeds the **source alert in the header** (`.admin-alert.mt-2 > .d-flex.flex-row-reverse` with `.avatar img[gravatar]`, `span.created-at`, `strong.username`, `.msg-left.text-formated.preText` body); modal is **non-dismissible** (`data-keyboard=false`, `data-backdrop=static`); body empty state "There are no questions." Ours is richer (listQuestions/postQuestion/resolveQuestion, admin-gated answer/resolve) — a divergence from ST's one-way ask. — Section 2.
  4. `app-all-user-pmmodal` (`#all-user-pm-modal`) captured **only in loading state**: `.text-center.my-4 > h5 > i.fas.fa-spinner.fa-spin "Loading..."`; loaded thread-row list NOT captured — honest gap. **NEW** component needed; backend needs a "list my PM threads" endpoint. — Section 3.
  5. `app-privchat` (`#privaChatCompHolder.privChatHolder`) is a **docked panel, NOT a Bootstrap modal**: `nav.navbar.chat-nav-pm` chrome with `a.navbar-brand > i.fas.fa-comments`, a settings gear dropdown (`i.fas.fa-cog.chat-header-gear`), a close `i.fas.fa-times`; `.pc-body` empty state "No active chat". **NEW** component. Muted (file20) and Followed (file21) modals captured **empty-state only** ("You don't have any muted/ignored users." / "...followed users."), list-row + unmute/unfollow actions NOT captured. — Sections 4, 5, 6.
- **notes**: Four surfaces (all-user-pm, muted, followed populated rows, privchat active state) are captured only in loading/empty state — explicit gaps that cannot be filled from these dumps and must be treated as honest-pending, not invented. The gravatar avatar in the alert-qa header and the "one-way ask, no answer UI" claim should be re-verified against file19 raw DOM. Distinguishes a docked panel (privchat) from Bootstrap modals — a structural fact worth preserving.

---

## Cross-file notes
- All three are **prose transcriptions of `files/fileN.html` Angular snapshots**, not the JSON captures. The authoritative check for every id/class/href/color here is the corresponding raw HTML DOM dump (file5,7,8,9,10,11,14,16,17,18,19,20,21,27,32) and, for computed styles/rects, the JSON captures — none of which these files contain.
- Multiple surfaces are documented **only in loading/empty state** (all-user-pm, muted rows, followed rows, privchat active, several "empty in snapshot" modal bodies). Treat every populated/loaded layout as an uncaptured gap.
- The settings file's appearance table records **Angular binding expressions as `value`s** — usable for control ids/labels, useless for default color values (get those from a JSON capture's computed styles).
