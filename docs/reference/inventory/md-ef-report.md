# report.md

- **path**: `evidence-folder/report.md`
- **kind**: **md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps.** This is prior analysis (11 parallel agents' reconstruction), not a raw capture. Rendered JSON/HTML dumps always win over anything asserted here.
- **size**: 248,207 bytes (248 KB); 3,319 lines. Read fully (structure mapped via header grep + full read of §01–§02 and targeted reads of §06, §08, §10, §11).
- **role**: **admin/mixed** — self-declares its source as the `admin-room` capture (label `admin-room`, line 3), BUT §00/§02 caveats state the captured session is actually a **viewer/member** session, not presenter. So the prose describes an admin-labeled capture that is functionally a member view; presenter surfaces are explicitly reconstructed/inferred, not captured. Determined from line 3 (`label admin-room`) + lines 6, 275-279 (four-item viewer navbar cluster, presenter controls "NOT in this DOM").
- **format/quality**: prose analysis / reference spec. Narrative + tables citing computed styles, rects, class names, and design tokens that were *derived from* the raw slices — it is a written interpretation, not the raw data itself.
- **surfaces documented**: (11 sections) 01 Room Shell & Layout · 02 Top Nav Bar · 03 Left Sidebar/Roster Drawer · 04 Presentation Stage · 05 Webcams · 06 Alerts & Chat Dock · 07 Modals/Overlays/Dropdowns · 08 Controls Inventory (230) · 09 Targeted Exact CSS (50 matchedRules) · 10 Theme & Design Tokens (294 vars) · 11 Catch-all (284-node census).
- **maps to (our components)**: essentially the entire app — Room shell/split layout, TopNav, Sidebar/Roster, PresentationArea (tab strips mainTabs/screenTabs/streamsTabs/notesTabs/myTab), Webcams, Alerts panel, Chat panel + composer, Modals, and the theme/token layer. It is the umbrella narrative that the per-slice raw dumps back (or contradict).

## Key findings (cited)

1. **Source & scale.** Line 3: reconstructed from `proroom-ultra-admin-room.json` (15 MB, viewport **1989×1166** dpr 2, label `admin-room`, title "Mastering The Trade") — "from 2,639 elements + 5 subtrees + 50 targeted (matchedRules) + 230 controls." So the underlying raw authority is that JSON plus its `_slices/*` (elements-alerts.json 2349 nodes cited at line 1191, elements-chat.json `[]` empty at line 7, subtree-roomShell.json, subtree-topnav.json, subtree-presentation.json 553 elems/50 captured at line 718, targeted.json, controls.json, theme.json).

2. **BADGE GAP — confirms the NON-NEGOTIABLE warning.** `grep -niE 'user-badge-img'` over this file returns **ZERO hits**. The only "badge" nodes it documents are the Files count pill (`span.badge.rounded-pill.bg-danger.files-badge`, line 814/943) and the alert-row Q&A "?" button (`button.alert-qa`, line 1338). Roster role/badge is only *inferred* — line 684: "Each row ... is **expected** to carry: avatar/presence dot, username, role/badge" because "Per-user rows are NOT in this slice ... `div.room-roster-list` is empty." So this prose does NOT contain the real `<img class="user-badge-img">` badges; treat all badge claims as unverified against the rendered DOM.

3. **Self-flagged internal contradictions (do NOT trust prose here).** Line 8 caveat: §06 reports NO kebab on alert rows but §08 found **124 `#dropdownMenuLink` kebabs** (line 1908/2041); and §06 *infers* chat own-message = right-aligned `flex-row-reverse` (lines 1426, 1437) which "conflicts with the all-left rule we verified across 250+ rendered rows + your directive." The file itself says treat §06 chat claims as inferred (chat slice empty, `elements-chat.json = []`, line 7) — verify against a real screenshot/presenter capture.

4. **Layout constants (load-bearing, but derived).** Navbar fixed `#0c2434`, 49px tall, z-index 1030 (lines 91, 105, 328). Main horizontal split `as-split#mainAreaSplit` = **21.24% / 78.76%** left/right with an 11px `col-resize` gutter `#0a6db1` (lines 117-121). Left column 420.055px (`calc(21.2364% - 2.336px)`), right presentation-box 1558px, bg `--presenter-area-bg #0f2e43` (lines 135-137). Inner vertical split ~81.4% alerts / ~18.6% chat, alert-box 900.305px — but chat-box/app-chat geometry is **inferred** (empty at capture, lines 68, 179).

5. **Theme tokens (§10).** `:root` carries **294** custom properties = 123 `--bs-*` + 171 app-specific (line 2617). Base is Bootstrap 5.3 "Darkly"-derived (`--primary:#375a7f`, `--success:#00bc8c`) with a custom navy/blue chat skin; three brand anchors recur: **`#45a2ff`** (accent/link/active), **`#0a6db1`** (header/username), **`#103d5c`** (sidebar/modal navy) (line 2625). Ships dark + light token families in parallel (`darkTheme-*`/`lightTheme-*`); admin screenshot is the dark variant. Icon system is Font Awesome 5 (`fa-*`), which the report itself says to map to `phosphor-svelte *Icon` on rebuild (line 2617 note).

## Notes

- **NOT authority — SECONDARY/PROSE.** Per assignment rule 0, this is prior analysis and contains known errors (missing `user-badge-img`; the disputed `flex-row-reverse` own-message inference; kebab presence disagreement between §06 and §08). Every claim here must be re-verified against the raw JSON capture (`proroom-ultra-admin-room.json` + `_slices/*.json`) and raw HTML DOM dumps before use.
- **Superset / index role.** This is the single largest prose synthesis in the corpus — it is effectively the human-readable index/superset that stitches together the per-slice raw dumps. Best used as a map of *where to look* in the authoritative dumps, not as a fact source.
- **Honest gaps the file itself admits:** chat panel empty at capture (`elements-chat.json = []`); presenter broadcast controls absent (viewer-session capture) and reconstructed as TODO (§3.6, line 440-454); roster rows empty; several chat/alert details flagged "inferred." No corruption or duplication within the file itself; it is internally consistent prose with self-noted cross-section conflicts.
