# md-file2 — inventory of docs/reference/file2-*.md

> **Provenance note (applies to all three):** these files are **kind: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps.** They are not present in the working tree (`git status` shows them as `D` deleted); all three were read from `HEAD` (`git show HEAD:docs/reference/<file>`). Each is a hand-cleaned prose extraction of `mixed-files/file2.html` — a **5,909,581-byte / 84,070-line** Angular-17 rendered snapshot of the Simpler Trading / ProTradingRoom room. They cite exact line ranges into that HTML, but Angular cruft was stripped and the markup was *simplified by a human*, so the HTML dump — not these — is the authority. Rendered capture / raw DOM always wins over this prose.
>
> **Source-marker verification** (grep counts in `mixed-files/file2.html`, confirming these analyses describe real content): `app-st-message`=102, `alert-qa`=53, `created-at`=52, `app-alerts`=4, `mainTabs`=3, `mainTabset`=1, `noteTabset`=1, `files-badge`=3, `alertHeader`=1, `chatHeader`=1, `app-poll-modal`=2, `alerts-advanced-search`=4, `app-webcam-holder`=2. **`user-badge-img`=0** in file2.html — consistent with file2-alerts.md's claim "No stars/badges on usernames in this snapshot," and a reminder that the `<img class="user-badge-img">` badges called out in the corpus-wide NON-NEGOTIABLE are NOT in this particular capture.

---

# file2-alerts.md
- **path**: docs/reference/file2-alerts.md
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps
- **size**: 10,460 bytes (from `git cat-file -s HEAD:...`; file deleted from working tree, read via `git show HEAD:`)
- **role**: member-facing surface, but the *source* file2.html is a presenter/admin capture (mixed) — determined because file2-modals.md (same source HTML) documents admin-only surfaces (poll creation, alert-send-report, advanced search). This file itself covers the read-only member Alerts panel.
- **format/quality**: prose analysis of raw DOM + inline styles (simplified markup trees, tables). NOT computed-styles/rects — inline `style="..."` values only.
- **surfaces documented**: `<app-alerts>` panel — header (`alertHeader` navbar: bell title, search, settings gear), `<app-roomscroller id="chatScrollViewParentAlerts">`, one alert row `<app-st-message>` (day `.separator`, `.msg-box`, `.msgMenu` options dropdown `.users-dropdown-options`, avatar, `.username`, `.alert-qa` Q&A badge, `.created-at`, `.msg-left.preText` body, `.img-container`/`.uploaded-img`, `.linkColor` autolink).
- **maps to (our components)**: `web/src/lib/components/AlertFeed.svelte` (primary; the file asserts it already mirrors the structure), plus `AlertsChatDock.svelte` (day separators, `role="separator"` lines 126/148 per this file).
- **key findings** (cited):
  1. **Alerts panel is read-only — no composer** anywhere in cited lines 683–12315 (no `<form>`/`<textarea>`/`<input>`/Post). Flagged as an action item because pro-room's `AlertFeed.svelte` DOES ship a composer (`.sym`/`.note`/Post, ~lines 267–273) — a deliberate pro-room addition, not a fidelity match. (finding box + §4 + §6.1)
  2. **Author-color readability hack**: `.msg-box` gets inline `background-color`; `.username`/`.created-at`/`.msgMenu` carry that same bg color **plus `filter: invert(1)`**; body/`header` wrapper carry the author's true text color e.g. `rgb(0,128,64)` green. Example row: `background-color: rgb(215,215,215)`, author "LornaBot", stamp "6/8/26, 8:00 AM". (§3 + Per-row details)
  3. **Options menu exact items**: User Info (`fas fa-user`), Mention (`fas fa-reply`), Copy (`fas fa-copy`) — with 6 empty `<!---->` slots between Mention and Copy (permission-gated Edit/Delete/Pin hidden for viewer). (§3 Options menu table)
  4. **Q&A badge** `<button class="alert-qa">` renders only on alerts with questions: `<span>(N)</span>` count + `fa-question-circle` + optional `<span>✅</span>` when resolved. (§3 Q&A badge)
  5. **No cashtag linkification** — tickers (SPX, AAOI) are plain text; only explicit URLs become `<a class="linkColor" onclick="event.stopPropagation()">`. Avatar `<img alt="msg.avt">` has no width/height (CLS). Source has a `clas="…"` typo (dead class) — do not copy. (§3 details + §6.4/6.5/6.6)
- **notes**: Best-authority for the Alerts panel structure among the .md corpus, but STILL secondary — verify every claim against `mixed-files/file2.html` lines 683–12315 (102 `app-st-message`, 53 `alert-qa`, 52 `created-at` confirm the row content exists). Header settings-dropdown menu items are NOT in the snapshot (closed state) — an honest gap, not constrained by this reference.

---

# file2-mainstage.md
- **path**: docs/reference/file2-mainstage.md
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps
- **size**: 15,580 bytes (`git cat-file -s HEAD:...`; deleted from working tree, read via `git show HEAD:`)
- **role**: mixed — covers member-facing MainStage/Chat/Notes/Files plus presenter webcam tiles. Same source HTML (file2.html) which is a presenter/admin capture. Determined from surfaces (webcam holder, presenter cams) + shared source with the admin modals file.
- **format/quality**: prose analysis of raw DOM + inline styles (simplified markup trees + tables). Inline styles only, no computed styles/rects.
- **surfaces documented**: (1) MainStage tab bar `<app-presentationarea>` `#mainTabs.mainTabset` (Screens/Streams[hidden]/Notes/Files); (2) `#screens` empty "No one is presenting" state + `#screenTabs`/`#screensTabsContent`; (3) Notes `#notesTabs.noteTabset` + `#notesTabsContent` (`app-note`, `.noteDownload`); (4) Files `#myTab.files-tabs` (Files/Images/Sounds + `.files-badge` counts, `.st-searchbar` search, `.st-fileSeeMore` Refresh); (5) Chat `<app-chat>` `.chatHeader` (Main Chat/Off Topic tabs, search, gear, `<app-roomscroller>`, `#textAreaHolder` composer); (6) Webcam holder `<app-webcam-holder>`/`<app-presenter-cams>`.
- **maps to (our components)**: `MainStage.svelte` (tab shell), `ScreenStage.svelte` (empty state + presenter tile + zoom/volume), `NotesPanel.svelte`, `FilesPanel.svelte`, `ChatPanel.svelte`, `WebcamHolder.svelte` — mapping table in §7.
- **key findings** (cited):
  1. **Streams tab is `hidden`** in DOM; four tabs Screens(`fa-desktop`,active)/Streams(`fa-podcast`,hidden)/Notes(`fa-edit`,`#noteChangeIndicator`)/Files(`fa-folder`). Active tab = `nav-link active`+`aria-selected="true"`; inactive add `tabindex="-1"`. (§1 table, cites lines 12596–12745)
  2. **Zoom/volume controls are NOT in the captured DOM** — `#screenTabs`/`#screensTabsContent` are empty (not-presenting state); zoom (`fa-search-plus/minus`) + volume (`fa-volume-*`) render only on a live tile → must be reconstructed, honest gap. Hidden global audio `<audio id="mp3player" autoplay src="">` at line 16300. (§2)
  3. **8 named notes**, order captured: Welcome / JC's Daily Briefing / Henry's Workflowy Notes / Sam's Mag 7 index / Taylor's Scorecard Rankings (6/02 CLOSE) / +more. Only "Welcome" has `nav-link active` + `.badge.badge-success` home badge (`fa-home`, id `652765a0e494735aa53574ba-tab`, ngbtooltip "Welcome Mat…shown by default when noboby[sic] is presenting"). Per-note action = single green `.noteDownload` button; **no version-history affordance** found. (§3a/§3b)
  4. **Files pane is EMPTY** — Files/Images/Sounds `.files-badge` counts all `0`, no file rows in the snapshot; row template (name/size/date/green Download) renders dynamically → reconstruct, honest gap. (§4a/§4c)
  5. **Chat composer collapsed** — only a single `fa-plus` ("Show message options") button present; emoji/image/GIF are the un-expanded expansion of `.textAreaBtns`, NOT in DOM → reconstruct. Header tabs Main Chat(active)/Off Topic. Webcam tiles templated: `webcamsHolder-`/`webcamVideo-` get presenter-id suffix, `.pNameLabel` + `.closeIcon` `fa-times`. (§5 + §6)
- **notes**: Best-authority .md for the MainStage/Chat/Notes/Files/Webcam layout, but secondary — verify against file2.html (`mainTabset`=1, `noteTabset`=1, `files-badge`=3 confirmed by grep). §7 gives a full source-DOM→Svelte-component map plus a FontAwesome→phosphor `*Icon` translation table (to be verified against `node_modules/phosphor-svelte`).

---

# file2-modals.md
- **path**: docs/reference/file2-modals.md
- **kind**: md-analysis — SECONDARY/PROSE, NOT authority, must be verified against the raw dumps
- **size**: 17,279 bytes (`git cat-file -s HEAD:...`; deleted from working tree, read via `git show HEAD:`)
- **role**: presenter/admin (mixed) — determined because it documents admin/presenter-only surfaces: poll authoring, alert logs listing "By: admin@protradingroom.com", alert-send-report, advanced search, local screenshare preview. Confirms file2.html is a privileged capture, not a plain member view.
- **format/quality**: prose analysis of raw DOM + inline styles (indented markup trees + control tables). Inline attrs/styles only; no computed styles/rects.
- **surfaces documented**: 7 overlays — (1) `<app-poll-modal>` floating panel `#pollModalCompHolder.pollModalHolder` (Create New Poll / Pre-Canned tabs); (2) `<app-alert-logs-modal>` `#alerts-logs-modal`; (3) `<app-alert-qa-modal>` `#alertQAModal`; (4) `<app-screenshare-preview>` `#screenshareLocalPreviewHolder` (draggable+resizable card); (5) `<app-alert-send-report-modal>` `#alert-send-report-modal`; (6) `<app-alerts-advanced-search>` `#alerts-advanced-search-modal`; (7) `<app-alert-filter-modal>` `#alert-filter-modal`.
- **maps to (our components)**: `PollModal.svelte`/`PollPanel.svelte`; `web/src/lib/components/modals/AlertLogsModal.svelte`; `AlertQaModal.svelte`; `ScreenStage.svelte`/`WebcamHolder.svelte` (screenshare preview); **NEW** `AlertSendReportModal.svelte` (flagged as not existing); `web/src/lib/components/modals/AdvancedSearchModal.svelte`; `web/src/lib/components/modals/AlertFilterModal.svelte`.
- **key findings** (cited):
  1. **Poll modal is a windowed floating panel, not a Bootstrap `.modal`** — custom titlebar with Minimize/Maximize/Close (`fa-window-minimize`/`fa-window-maximize`/`fa-times`), two tabs `#sendpoll` "Create New Poll" + `#savedPolls` "Pre-Canned Polls", numbered `label-warning` step badges 1/2/3, `#pollQuestionTxt`, `#pollChoiceTxt`+Add Choice (`fa-plus-circle`), `#anonymous-poll` checkbox, "Save To Canned" (`fa-floppy-o`) + green "Send Poll". (§1, lines 18528–18759)
  2. **`AlertSendReportModal.svelte` does NOT yet exist** (explicitly NEW) — `#alert-send-report-modal`, title "Alert Sent Report. AlertID:", body async-loaded (snapshot shows only `fa-spinner fa-spin` "Loading..."), read-only + Close. (§5, lines 83087–83150)
  3. **Advanced search** `#alerts-advanced-search-modal` enumerates **20 traders** (Allison…CML Alert Bot) and **3 rooms** (Showcase Room / Mastering The Trade / Tr3ndy Trading) as `data-bs-auto-close="outside"` multi-selects; checkboxes `#checkNonTradeAlert` + `#checkArchives`; **`datetime-local`** `#startDateInput`/`#endDateInput`; header "Rooms" refresh (`fa-sync-alt`); empty results "No logs to display. Please, change the input fields." (§6, lines 83219–83685)
  4. **Alert Q&A modal** `#alertQAModal` uses `data-backdrop="static"` + `data-keyboard="false"` (no ESC close); header **embeds the source alert** (avatar/`.username`/`.created-at`/`.msg-left.preText`); empty state "There are no questions."; composer `#textAreaQATxt` (rows=1, placeholder "Type your question here...") + emoji (`far fa-smile`) + image (`fas fa-image`), no explicit Send button (Enter-to-submit). (§3, lines 82516–82688)
  5. **Screenshare preview is draggable+resizable** (jQuery-UI `ui-draggable`+`ui-resizable`, 8 handles n/e/s/w/ne/se/sw/nw), NOT a modal — `#screenshareLocalPreviewHolder.card.webcamsHolderScreen`, source dropdown `#dropdownBasic1`, close `fa-times`, `<video id="webcamScreenLocalPreview" autoplay>`. Alert-filter modal `#alert-filter-modal` toggles allowlist/blocklist via `#show-alerts` checkbox ("Only show alerts from these people:"), empty "List is empty.", footer `justify-content-between`. (§4 lines 82806–82884, §7 lines 83686–83762)
- **notes**: Best-authority .md for the modal/overlay inventory (7 surfaces with exact ids/line ranges), but secondary — verify against file2.html (`app-poll-modal`=2, `alerts-advanced-search`=4 confirmed by grep). Line ranges (18528, 82158, 83686) are plausible within the 84,070-line source. Several modals were captured only in loading/empty state → resolved-state branches (empty `<!---->` siblings) are honest gaps.
