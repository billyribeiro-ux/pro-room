# Rust
/server/target
**/*.rs.bk

# Node
node_modules/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.log

# Build artifacts
/web/build
/web/.svelte-kit

# Local reference captures — large Angular DOM dumps used to reverse-engineer the
# 1:1 clone. Kept locally for discovery; never committed (tens of MB, not source).
/files/
/important-doc.html

# local redis dev artifact
dump.rdb
*.rdb

# admin-room reference-spec: report.md is the committed artifact; the rest are
# bulky/regenerable intermediates (15MB+ raw capture, sliced JSON, section fragments).
evidence-folder/_slices/
evidence-folder/_report-sections/
evidence-folder/*.json
